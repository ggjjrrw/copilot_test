# MyHome
## 工程初始化要求
计划基于JAVA Springboot框架，做一个http后端服务服务，要求初始化这个工程，具体工程初始化要求包括：
* 整个工程是个maven工程
* 要求使用罪行的Springboot框架
* 要求先完成一个healthz接口，用于作为服务启动成功，健康检查的接口
* 整个http服务的端口设置为8080
* 创建build目录，该目录中包含Dockerfile文件，完成服务镜像的构建
* 创建manifests目录，为项目的helm chart目录
